# Projeto "CarMatch" - Nome temporário

### Configuração do Ambiente

#### Comando para instalar dependências

``sudo pacman -S python-pip podman podman-compose postgresql-libs``

``sudo pacman -S nodejs npm``

Esse comando irá instalar globalmente as dependências do sistema.

``cd pasta_do_projeto``

Entrar na pasta do projeto

``sudo ``